import * as React from "react";

const DateBody = (props) : JSX.Element => {
    // 暂时使用task.all 渲染日历列表
    const { curDate, tasks, handleAddDate, handleSeletctDate } = props;

    return (<>
    待续
    </>)
}

export default DateBody;